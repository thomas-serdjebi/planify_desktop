{% extends 'base.html.twig' %}

{% block title %}Livraison{% endblock %}

{% block body %}
    <h1>Livraison</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ livraison.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ livraison.numero }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ livraison.adresse }}</td>
            </tr>
            <tr>
                <th>Code_postal</th>
                <td>{{ livraison.codePostal }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ livraison.ville }}</td>
            </tr>
            <tr>
                <th>Client_nom</th>
                <td>{{ livraison.clientNom }}</td>
            </tr>
            <tr>
                <th>Client_prenom</th>
                <td>{{ livraison.clientPrenom }}</td>
            </tr>
            <tr>
                <th>Client_email</th>
                <td>{{ livraison.clientEmail }}</td>
            </tr>
            <tr>
                <th>Client_telephone</th>
                <td>{{ livraison.clientTelephone }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ livraison.date ? livraison.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Creneau</th>
                <td>{{ livraison.creneau }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ livraison.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_livraison_index') }}">back to list</a>

    <a href="{{ path('app_livraison_edit', {'id': livraison.id}) }}">edit</a>

    {{ include('livraison/_delete_form.html.twig') }}
{% endblock %}
